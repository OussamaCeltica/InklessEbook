<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.devs.celtica.inkless">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/inkless_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/inkless_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.SearchBook"  ></activity>
        <activity android:name=".Publications.OpenPdf" />
        <activity android:name=".Publications.AfficherBooks" />
        <activity android:name=".Activities.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.SignUp" />
        <activity android:name=".Activities.Accueil" />
        <activity android:name=".Publications.UploadPdf" />
        <activity
            android:name=".Users.Profile" />
        <activity android:name=".Publications.ProfileBook" />
        <activity android:name=".Publications.UploadAudio" />
    </application>

</manifest>